<view class="page">
  <view class="section-title">预约信息</view>
  <view class="card form">
    <view class="field">
      <view class="label">自取时间</view>
      <picker mode="time" value="{{pickupTime}}" bindchange="onPickupTimeChange">
        <view class="picker">{{pickupTime || '请选择时间'}}</view>
      </picker>
    </view>
    <view class="field">
      <view class="label">联系电话</view>
      <input type="number" value="{{contactPhone}}" placeholder="用于摊主联系你" bindinput="onPhoneInput" />
    </view>
    <view class="field">
      <view class="label">备注</view>
      <textarea value="{{remark}}" placeholder="口味、忌口或其他说明" bindinput="onRemarkInput" />
    </view>
  </view>

  <view class="section-title">选择商品</view>
  <view wx:if="{{!items.length}}" class="empty">当前没有可预约商品</view>
  <block wx:for="{{items}}" wx:key="_id">
    <view class="card item">
      <view class="item-main">
        <view class="row between">
          <view>
            <view class="item-name">{{item.name}}</view>
            <view class="muted">￥{{item.priceText}}</view>
          </view>
          <view class="qty">
            <button data-id="{{item._id}}" bindtap="minus">-</button>
            <text>{{item.quantity}}</text>
            <button data-id="{{item._id}}" bindtap="plus">+</button>
          </view>
        </view>

        <view wx:if="{{item.quantity > 0 && item.flavorOptions.length}}" class="flavors">
          <view class="flavor-title">选择口味{{item.flavorMultiSelect ? '（可多选）' : '（单选）'}}</view>
          <view class="flavor-options">
            <button wx:for="{{item.flavorOptions}}" wx:for-item="flavor" wx:key="*this" size="mini" data-id="{{item._id}}" data-flavor="{{flavor}}" bindtap="toggleFlavor">{{flavor}}</button>
          </view>
          <view class="muted selected-text">已选：{{item.selectedFlavorText || '未选择'}}</view>
        </view>
      </view>
    </view>
  </block>

  <view class="summary row between">
    <view>合计：￥{{totalAmount}}</view>
    <button class="primary-btn submit" loading="{{isSubmitting}}" bindtap="submitOrder">提交并支付</button>
  </view>
</view>
